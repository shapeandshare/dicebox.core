{
  "name": "dicebox.core",
  "version": "3.6.0",
  "description": "Core libraries for the DiceBox API platform",
  "scripts": {
    "lint": "black -l 120 . --check && pylint",
    "lint:fix": "black -l 120 .",
    "build": "npm run clean && npm run build:wheel",
    "build:wheel": "python setup.py bdist_wheel",
    "build:image": "docker build . -t dicebox.core",
    "clean": "rimraf htmlcov .coverage build dist src/shapeandshare_dicebox_core.egg-info",
    "test": "npm run test:unit && npm run test:coverage",
    "test:unit": "coverage run --branch -m unittest discover -s test -p '*Test.py'",
    "test:coverage": "coverage report -m && coverage xml && coverage html"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
